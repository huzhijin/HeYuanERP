# HeYuanERP 打印相关环境变量示例（请复制为 .env 并按需修改）

# 打印引擎：playwright 或 puppeteer（默认 playwright）
PRINT_ENGINE=playwright

# 模板根目录（相对可基于应用目录，或使用绝对路径）
# 默认：<AppBase>/assets/templates
PRINT_TEMPLATES_ROOT=assets/templates

# 模板参数快照目录（用于重打/审计复现）
# 默认：<AppBase>/var/print-snapshots
PRINT_SNAPSHOT_DIR=var/print-snapshots

# 默认模板名（当未指定 template 参数时）
PRINT_DEFAULT_TEMPLATE=default

# PuppeteerSharp 可选：显式指定 Chromium 可执行文件路径
# CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium

